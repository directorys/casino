// Раскрывающееся меню
export const infoHeader = Array.from(document.querySelectorAll('.info__header'));
export const infoPunkts = Array.from(document.querySelectorAll('.info__punkts'));
export const infoImage = Array.from(document.querySelectorAll('.info__image'));